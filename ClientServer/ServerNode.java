public class ServerNode {
    // ToDo: Can likely be almost entirely copied from P2P/p2pNode.java
    // ToDo: Update to follow documentation and edit for that

        // ToDo: Implement new logic to config, server.ip/port for actual server, client(n).ip/port for each client node
}
