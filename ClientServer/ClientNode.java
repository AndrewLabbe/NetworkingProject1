public class ClientNode {
    // ToDo: can use much of p2pNode.java, but only sends heartbeats to server
    // ToDo: Does not need to listen to client nodes


    // ToDo: Implement new logic to config, server.ip/port for actual server, client(n).ip/port (identify self)
}
